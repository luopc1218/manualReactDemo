import React from 'react';
import ReactDOM from 'react-dom';
import App from './app.jsx';

ReactDOM.render(<App />, document.getElementById('root')); //    告诉react渲染什么，在哪儿渲染。这里我们将要渲染一个叫做APP（马上就要创建了）的组件，渲染到我们index.html中的一个div中。
